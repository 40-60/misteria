{
  "name": "test",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run build && bunx tsc --outDir dist --rootDir src --allowJs --watch src/**/*.ts src/*.ts src/**/*.js",
    "build": "rm -rf dist && bunx tsc --outDir dist --rootDir src src/**/*.ts src/*.ts",
    "serve": "bunx serve ."
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
